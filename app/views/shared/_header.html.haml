.header
  .brand
    = link_to 'Now', root_path
    |
    = link_to 'Then', '#'
    |
    = link_to 'Later'

  .nav
    = link_to 'Profile', show_profile_path(current_user.user_id)
    |
    - if user_signed_in?
      = link_to 'Sign out', destroy_user_session_path, method: :delete
    - else
      = link_to 'Sign in', new_user_session_path