.entry_list
  Current entries
  - @entries.each do |entry|
    .entry_item
      .entry_title
        = link_to entry.list_title, show_entry_path(entry.id)
      .entry_tags
        = entry.tags
  .nav
    Archived
    |
    = link_to 'Sign Out', destroy_user_session_path, method: :delete